
    drop table if exists State ; 

    create table State (
        id integer generated by default as identity (start with 1),
        capital varchar(255),
        name varchar(255),
        region varchar(255),
        type varchar(255),
        primary key (id)
    )